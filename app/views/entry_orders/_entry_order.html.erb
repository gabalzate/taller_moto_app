<div class="bg-white shadow-sm rounded-lg p-4">
  <p><strong class="font-medium">Descripción del problema:</strong> <%= entry_order.problem_description %></p>
  <p><strong class="font-medium">Kilometraje:</strong> <%= entry_order.mileage %></p>
  <p><strong class="font-medium">Nivel de combustible:</strong> <%= entry_order.fuel_level %></p>
  <p><strong class="font-medium">Nivel de aceite:</strong> <%= entry_order.oil_level %></p>
  <p><strong class="font-medium">Notas:</strong> <%= entry_order.notes %></p>
  <% if entry_order.photos.attached? %>
    <div class="mt-4">
      <h4 class="font-semibold">Fotos:</h4>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-2">
        <% entry_order.photos.each do |photo| %>
          <%= image_tag photo, class: "rounded-lg shadow-md" %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
