<div class="container mx-auto p-4 max-w-lg">
  <div class="bg-white shadow-lg rounded-lg p-6">
    <h1 class="text-3xl font-bold mb-4 text-gray-800">Cliente: <%= @client.name %></h1>

    <p class="text-gray-600 mb-2">
      <strong>Email:</strong> <%= @client.email %>
    </p>
    <p class="text-gray-600 mb-2">
      <strong>Número de Documento:</strong> <%= @client.document_number %>
    </p>
    <p class="text-gray-600 mb-2">
      <strong>Teléfono:</strong> <%= @client.phone_number %>
    </p>

    <div class="mt-6 flex space-x-4">
        <%= link_to 'Editar Cliente', edit_client_path(@client), class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-amber-600 hover:bg-amber-700" %>

        <%= link_to 'Eliminar Cliente', client_path(@client), class: "inline-flex items-center px-4 py-2 border border-red-300 text-sm font-medium rounded-md text-red-700 bg-white hover:bg-red-50", data: { turbo_method: :delete, turbo_confirm: '¿Estás seguro de que quieres eliminar este cliente?' } %>

        <%= link_to 'Volver a la lista', clients_path, class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
    </div>

    </div>
  </div>
</div>
